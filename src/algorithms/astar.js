// A* Search Algorithm
//  - Inputs
//      - Adjacency Matrix
//      - Start Node
//      - Goal Node
//  - Output:
//      - Guarenteed shortest possible path

export function astar(grid, start, goal){
    if(!start || !goal || start == goal)
        return false;

    

}